language: ruby
addons:
  apt:
    packages:
      - haveged
rvm:
  - 2.4
  - 2.5
  - 2.6
  - 2.7
env:
  global:
    - RUBY_ENV=test
  matrix:
    - REEK_VERSION=3.0
    - REEK_VERSION=4.0
    - REEK_VERSION=5.0
    - REEK_VERSION=6.0
