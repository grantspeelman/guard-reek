language: ruby
addons:
  apt:
    packages:
      - haveged
rvm:
  - 2.3
  - 2.4
  - 2.5
  - jruby-9.1.12.0
env:
  global:
    - RUBY_ENV=test
  matrix:
    - REEK_VERSION=3.0
    - REEK_VERSION=4.0
    - REEK_VERSION=5.0
